# Example unit test using PHP

## Running

* clone and install dependencies

```
~$ git clone git@github.com:arseto/php-test-example
~$ composer install
```

* run unit test with raw php

`~$ php -e test.php`

* run unit test with phpunit

`~$ vendor/bin/phpunit`

